#include <stdio.h>

int main() {
    int n;
    int vector[1000];
    
    scanf("%d", &n);
    for (int i = 0; i < n; i++) {
        scanf("%d", &vector[i]);}
    int ultimulElement;
    scanf("%d", &ultimulElement);
    for (int i = 0; i < n; i++) {
        if (vector[i] % ultimulElement == 0) {
            printf("%d ", vector[i]);        }
    }
    return 0;
}
